import 'package:flutter/material.dart';

class BmPengajuanPage extends StatelessWidget {
  const BmPengajuanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      padding: const EdgeInsets.all(24.0),
      children: [Text("PENGAJUAN EVENT")],
    );
  }
}
